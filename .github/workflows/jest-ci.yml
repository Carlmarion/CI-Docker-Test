name: jest-and-docker-ci

on: push

jobs:
  test-front:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2
      - name: Goto client and run tests
        run: cd client && npm i && npm test
  docker:
    needs: test-front
    if: github.ref == 'refs/heads/docker'
    runs-on: ubuntu-latest
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        name: Print dockerHub username
        run : echo ${{secrets.DOCKERHUB_USERNAME}}
        with:
          username: carlocarlo
          password: dckr_pat_kCN0H7coV0i0j-w2CvdDFxU0mh8
      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          push: true
          context: ./client
          tags: carlocarlo/quete1902:latest